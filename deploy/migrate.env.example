# Copy to a private migration env file (chmod 0600).
# Dedicated schema owner/migration role; this file is used by migrate only.
STUDIO_DATABASE_URL=postgresql+psycopg://studio_migrate:REPLACE_ME@DB_HOST:5432/flamoris_studio
